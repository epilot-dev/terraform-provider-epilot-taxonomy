// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package shared

import (
	"encoding/json"
	"fmt"
)

// EditMode - Controls whether a write goes through immediately or is held as a pending entry
// on the parent entity's `_changesets` map.
//
// - `direct`: write applied immediately. No changeset created.
// - `external`: write held as a pending changeset; auto-cleared on a matching direct/ERP write.
// - `approval`: write held as a pending changeset; requires explicit human apply/dismiss.
//
// Used at two levels: per-attribute (entity-attribute changesets) and per-capability
// (currently the `meter_readings` capability on the Meter schema, gating reading submissions).
type EditMode string

const (
	EditModeDirect   EditMode = "direct"
	EditModeExternal EditMode = "external"
	EditModeApproval EditMode = "approval"
)

func (e EditMode) ToPointer() *EditMode {
	return &e
}
func (e *EditMode) UnmarshalJSON(data []byte) error {
	var v string
	if err := json.Unmarshal(data, &v); err != nil {
		return err
	}
	switch v {
	case "direct":
		fallthrough
	case "external":
		fallthrough
	case "approval":
		*e = EditMode(v)
		return nil
	default:
		return fmt.Errorf("invalid value for EditMode: %v", v)
	}
}
